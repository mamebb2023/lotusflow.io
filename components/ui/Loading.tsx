import React from "react";

const Loading = () => {
  return (
    <span className="inline-block size-4 border-2 border-gray-400 border-t-transparent rounded-full animate-spin" />
  );
};

export default Loading;
